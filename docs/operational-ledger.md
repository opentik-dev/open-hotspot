# Open-HotSpot operational ledger

This is the append-only memory of target operations that would otherwise be
repeated. It records reusable command paths and failure classifications while
excluding credentials, FAS keys, router backups, and raw live-client data.

| ID | Date | Operation | Target state | Attempt | Result | Classification | Reuse rule | Evidence | Gate status |
|---|---|---|---|---|---|---|---|---|---|
| OP-001 | 2026-09-20 | Transfer r67 APK | EA8300/OpenWrt; SSH reachable; no SFTP server | `scp` to target | failure | environment | Do not retry `scp` while `sftp-server` is absent; use `tar` over verified SSH and move the artifact on-target. | `sftp-server not found` | Tested failure path |
| OP-002 | 2026-09-20 | r68 FAS diagnostic | openNDS 11/uhttpd | Bare FAS request treated every non-zero BusyBox wget exit as failure | failure | contract | HTTP 400 for a bare FAS request is an expected endpoint response; distinguish it from transport failure. | r68 field run | Superseded by r70 |
| OP-003 | 2026-09-20 | r69 FAS diagnostic | openNDS 11/uhttpd | Probe log was opened before its temporary directory existed | failure | implementation | Create the diagnostic workspace before any probe redirects output. | r69 field run | Superseded by r70 |
| OP-004 | 2026-09-20 | r70 install/diagnostic | EA8300 `boot_part=2`; openNDS 11 | `tar` over SSH, APK upgrade, read-only diagnostic | success | environment | Reuse the tar-over-SSH transfer path for this target; do not attempt SFTP. | APK SHA recorded in release history; diagnostic `failures=0 warnings=0` | Verified |
| OP-005 | 2026-09-20 | Fresh portal session | Physical phone client | Portal login produced an openNDS `Authenticated` client, one manager device, and one active session | partial | evidence | Treat Devices/Dashboard visibility as session-integration proof; still run close, usage, quota, and reboot gates separately. | User screenshots at 07:48/07:52; live openNDS counters non-zero while persistent usage remained zero | Verified; accounting pending |

| OP-006 | 2026-09-20 | Close/accounting probe | Acceptance target | First deauth probe failed in local command parsing before reaching SSH | failure | implementation | Do not infer a target result from a locally malformed probe; use the recorded status parser and record whether mutation was issued. | No target mutation occurred | Evidence pending |
| OP-007 | 2026-09-20 | r71 post-install reconciliation | EA8300/openNDS 11 | Read-only report observed zero live openNDS clients with one active SQLite session | partial | evidence | Treat as T011 stale-session evidence; do not mark the runtime chain green until close/restart policy reconciles it. | r71 diagnostic output; no credentials or client identifiers recorded | Pending Hardware Validation |

| OP-008 | 2026-09-20 | r72 adapter validation | EA8300/openNDS 11 | Installed versioned adapters and reran read-only diagnostics through the known tar-over-SSH path | success | environment | Reuse tar-over-SSH and require the versioned adapter check on future package installs. | r72 APK checksum; openNDS 11.0.0; FAS/uhttpd healthy; stale-session warning retained | Verified; T011 open |
| OP-009 | 2026-09-20 | r73 diagnostic contract | EA8300/openNDS 11 | Added an explicit packaged check that the daemon resolves to its versioned adapter | success | implementation | Require `adapter=versioned-opennds-contract` in every post-install report. | Repository and shell contract tests pass; target report shows adapter contract OK; stale-session warning retained | Verified; T011 open |
| OP-010 | 2026-09-21 | r74 report-driven consistency fixes | Repository candidate; target not changed | Corrected clean-checkout adapter test path, unified renewal-aware period selection, normalized ISO timestamps for BusyBox, and applied shared SQLite timeout/foreign-key defaults | success | implementation | Do not call these target-verified until the r74 APK is installed on the disposable slot and the runtime gates are rerun. Keep r60/r10.3.1 as rollback. | Full repository suite, PHP lint, APK rebuild, and BusyBox-compatible period/BinAuth tests pass | Tested; hardware validation pending |
| OP-011 | 2026-09-21 | r74 policy-change containment | Repository candidate; target not changed | Added adapter-backed deauthentication for live sessions affected by account suspend/delete, device block, or profile change | success | implementation | Verify on a disposable client; a source pass does not close T086/T088. | Repository contract suite after rebuild | Tested; hardware validation pending |
| OP-012 | 2026-09-21 | r74 release-gate check | Repository candidate | Ran the release gate checker after the source fixes | expected-block | governance | Do not tag or call r74 production while any listed physical gate is open; repeat only after new field evidence is attached. | `T003 T006 T011 T012 T052 T086 T087 T088 T090` reported open | Release blocked by hardware evidence |
| OP-013 | 2026-09-22 | r74 post-install/restart/reboot | EA8300 `boot_part=2`; r74 installed | r74 installed by tar-over-SSH; post-install and post-reboot diagnostics kept FAS/uhttpd/openNDS healthy, but zero clients plus one manager active session exposed the missing stale-session reconciliation; one early restart probe also failed locally due malformed SQL, then a separate readiness check passed | partial | evidence/implementation | Do not repeat the malformed combined probe. Use separate bounded readiness and database queries. Install the next reconciliation candidate before declaring T011 closed. | r74 diagnostic: failures=0, warning=manager-active-session-without-opennds-client; reboot returned openNDS 11.0.0/current clients 0; restore disabled | T011 remains Pending Hardware Validation |
| OP-014 | 2026-09-22 | r75 stale-session reconciliation | Repository candidate; target not yet changed | Added fail-closed reconciliation for disabled restore when a healthy openNDS status reports zero clients | success | implementation | Install r75 and verify that only the stale manager row closes; do not fabricate usage events or close while any openNDS client exists. | Local contract suite passed; APK SHA-256 `ec19fd6470c96bd45417e3799f9d1b613f1ba65f07d9f6db0a75137331ab778f`; target verification pending | T011 pending |
| OP-015 | 2026-09-22 | r76 pending-auth expiry | Repository candidate; target not yet changed | Added periodic transition of expired pending FAS transactions to `expired` so abandoned login attempts do not remain as live warnings | success | implementation | Install r76 and run one cycle/maintenance pass before judging the diagnostic warning. | Repository suite passed; APK SHA-256 `12547af79c3ee5ed4927f90b1853d2e90023cdc38ddaf1f63d4d3801a6444a1e`; target cleanup pending | T012 pending |
| OP-016 | 2026-09-22 | r76 live deauth control probe | EA8300/openNDS 11; one real authenticated client | Manager deauth passed the live MAC to `ndsctl`; target returned `Client not found` while status still reported the client Authenticated | failure | target contract | Do not repeat MAC-only deauth on this target. r77 resolves the live IP from status before calling `ndsctl deauth`, with MAC fallback only when no IP is available. | Target status remained authenticated and the manager session remained active; no client identifier recorded | T012 close/accounting remains pending |
| OP-017 | 2026-09-22 | r76 physical portal login | EA8300 `boot_part=2`; openNDS 11/uhttpd | Clean HTTP interception reached local FAS, valid credentials completed the handoff, and the same client became Authenticated with a manager device/session row | success | evidence | Preserve the clean HTTP/FAS sequence and test close, usage, quota, and reboot as separate gates. Do not use VPN/mobile data during acceptance. | FAS 200; handoff 307; openNDS Authenticated; manager active device/session present; no credentials or client identifiers recorded | T003 and login/session portion of T012 Verified; close/accounting pending |
| OP-018 | 2026-09-22 | r77 close callback investigation | EA8300/openNDS 11; one real authenticated client | IP-based deauth succeeded, but the manager row stayed active with zero usage events. The stock BinAuth log showed deauth `custom` as the base64 marker for `empty`; invoking the same callback with the saved session fields closed/accounted it successfully. | failure/root-cause found | target contract | Do not treat deauth rc=0 as accounting proof. r78 correlates only the explicit empty marker to the active MAC session and keeps unknown custom data fail-closed. | r77 deauth rc=0; automatic close missing; manual callback proof produced one usage event; no credentials or client identifiers recorded | T012 close/accounting remains pending until r78 physical retest |
| OP-019 | 2026-09-22 | r78 physical login/close/accounting | EA8300 `boot_part=2`; openNDS 11/uhttpd; Ethernet client | Installed r78 by tar-over-SSH, completed a fresh local-FAS login, verified Authenticated state, then invoked the package deauth control path; the target closed the callback automatically and SQLite recorded the new usage event. | success | evidence | Keep the r78 package and the empty-custom marker correlation. Do not infer quota, restart, or failure-containment acceptance from this single close path. | APK SHA-256 `c12a42df803b8ff50188a0849350510b0d9bb6ee71e684119d0af6a1b38ca57d`; FAS 200; handoff 307; deauth rc=0; clients 1→0; active sessions 1→0; usage events 1→2; diagnostic failures=0/warnings=0 | T003 and primary T012 path Verified; T006/T011/T052/T086/T087/T088/T090 remain open |
| OP-020 | 2026-09-22 | r78 T011 restart/reboot and service boundary | EA8300 `boot_part=2`; restore disabled then enabled | openNDS restart returned ready and closed the live session with one new usage event. Router reboot with restore enabled preserved the manager row but no client was restored until traffic appeared; manual restore then authenticated it. The manager service was inactive with no cron after boot, explaining the missing automatic path. | partial/failure | target and service boundary | r79 enables/starts the manager service on FAS activation and repairs enabled upgrades. Re-run reboot with service active; do not mark T011 closed from the manual restore. | Restart: ready=1, clients became 0, active sessions 0 after settling, usage events incremented; reboot: service inactive/no cron, then manual restore succeeded; no credentials or client identifiers recorded | T011 remains Pending Hardware Validation |
| OP-021 | 2026-09-22 | r79 T011 automatic restore | EA8300 `boot_part=2`; r79 installed; restore enabled for test then returned to disabled baseline | Live upgrade repaired and enabled the manager service; cron contained cycle and maintenance entries. After reboot, the manager session persisted, first HTTP traffic made the client visible, and the scheduled cycle automatically re-applied the policy without manual restore. | success | target evidence | Keep the service enabled whenever local FAS is enabled. Do not call T011 fully accepted until the duplicate-callback and complete restart matrix are attached. | r79 APK SHA-256 `d443169ed707f55d6412392ce666f96f9fe44f295731466da1937d05725eb1e2`; service enabled/active; cron entries=2; clients 0→1; state `Authenticated`; usage events unchanged during restore; final baseline restored to check interval 300 and restore disabled | T011 restore slice Verified; full T011 remains open |

## Current known facts

- The target transport path is SSH plus `tar`; the target does not provide
  `sftp-server`.
- The package is address-independent. Any displayed LAN address is field
  evidence only, not a configuration contract.
- `diagnose.sh` is read-only and is the first post-install command.
- A live session and live openNDS byte counters do not close the persistent
  accounting gate until the close callback creates a SQLite usage event.
